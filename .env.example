# Monorepo environment template
# Copy the app-specific templates:
# - backend/.env.example  -> backend/.env
# - frontend-app/.env.example -> frontend-app/.env

# This root file is optional and can be used for shared CI/CD vars.
ENVIRONMENT=development
